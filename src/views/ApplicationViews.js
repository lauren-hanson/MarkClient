import { Route, Routes } from "react-router-dom"

export const ApplicationViews = ({ token, setToken }) => {
  return (
    <>
      <Routes>
        <Route path="/">
        </Route>

      </Routes>
    </>
  )
}
